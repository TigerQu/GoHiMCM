gat:
  heads: 16  # Increased for RTX 5090 (was 8)
  dropout: 0.2  # Slightly reduced for more capacity
  elu_parameter: 1.0
ppo:
  discount_factor: 0.99
  gae_decay: 0.98
  clipping_param: 0.2
  entropy_coef: 0.01